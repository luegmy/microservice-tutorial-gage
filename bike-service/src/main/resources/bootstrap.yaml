spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

  application:
    name: bike-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/gage-realm
