spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: userdb
      username: admin
      password: p4ssw0rd
      authentication-database: admin
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
